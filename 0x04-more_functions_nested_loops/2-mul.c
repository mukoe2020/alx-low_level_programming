#include "main.h"

/**
  * mul - Entry point
  * Description - 'prints the results of two integers multiplied'
  * @x: first integer to be multiplied
  * @y: second integer to be multiplied
  * Return: result of multiplication
  */

int mul(int x, int y)
{
	int times = x * y;

	return (times);
}
